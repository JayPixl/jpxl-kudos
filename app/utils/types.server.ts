export interface RegisterForm {
    email: string;
    password: string;
    action: string;
    firstName: string;
    lastName: string;
}

export interface LoginForm {
    email: string;
    password: string;
    action: string;
}